# NOTE: nfl_data_py hard-pins pandas<2.0,numpy<2.0 in its own metadata, which
# has no prebuilt wheel for Python 3.12 and forces a broken source build.
# Its actual code works fine on modern pandas/numpy -- install it with
# --no-deps, then install everything below normally. See README Setup section.
nfl_data_py
pandas
numpy
pyarrow
scikit-learn
scipy
requests
appdirs
